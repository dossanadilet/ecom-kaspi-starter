{
  "dashboard": {
    "title": "Kaspi API Overview",
    "panels": [
      {
        "type": "graph",
        "title": "Requests",
        "targets": [
          {"expr": "sum(rate(http_requests_total[1m]))"}
        ]
      },
      {
        "type": "graph",
        "title": "Latency",
        "targets": [
          {"expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket[5m])) by (le))"}
        ]
      }
    ]
  }
}

